import java.util.*;
public class aggressive_cows{
    public static void main(String args[]){
        int arr[]={1,2,8,4,9};
        int cows=3;
        Arrays.sort(arr);
        int si=arr[0];
        int ei=arr[arr.length-1];
        int mid=si+(ei-si)/2;
        while(si<=ei){
            isSuited(mid)
        }
    }
}